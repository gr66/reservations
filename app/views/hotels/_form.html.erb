<%= simple_form_for(@hotel) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :stars %>
    <%= f.association :rooms %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
